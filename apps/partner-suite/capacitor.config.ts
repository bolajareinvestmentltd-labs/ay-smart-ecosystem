import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartassetz.partner',
  appName: 'SMART ASSETZ Partner',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
